module.exports = function () {
    /*
    //var db = require('../db/conexion.js')();
    const Schema = require('mongoose').Schema;
    const mongoose = require('mongoose');
    delete mongoose.connection.models['callQueue'];

    var callQueue = new Schema({
        number: String,
        idOperator: String,
        stated: String
    });
    //}, { bufferTimeoutMS: 600000 });
    return mongoose.model('callQueue', callQueue);
    /**/
};